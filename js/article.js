const C=window.GK_CONFIG||{};const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function fmtDate(d){return new Date(d||Date.now()).toLocaleDateString('en-GB',{day:'numeric',month:'short',year:'numeric'})}
function load(){let x=null;try{x=JSON.parse(sessionStorage.getItem('gk_article')||'null')}catch(e){};if(!x){$('#articleMain').innerHTML='<div class="empty">Article not found. Return to the homepage and open a story.</div>';return}document.title=x.title+' — GK Guide';$('#breadcrumbs').textContent='Home › '+(x.category||'Current Affairs')+' › '+x.title;$('#articleNav').innerHTML=['Home','India','World','Economy','Science & Tech','Environment','Sports','Static GK','Exams','More'].map((c,i)=>`<a href="${i===0?'index.html':'index.html'}" class="nav-item ${c===x.category?'active':''}">${esc(c)}</a>`).join('');$('#articleMain').innerHTML=`<img class="article-cover" src="${esc(x.image||C.FALLBACK_IMAGE)}"><div class="article-content"><h1>${esc(x.title)}</h1><div class="article-meta"><span class="category-pill" style="position:static">${esc(x.category)}</span><span>▣ ${fmtDate(x.eventDate)}</span><span class="verified-dot">Verified</span></div><p>${esc(x.summary)}</p><h3>Key Facts</h3><table class="keyfacts-table">${(x.keyFacts||[]).map(f=>{const p=String(f).split(':');return `<tr><td>${esc(p.shift()||'Fact')}</td><td>${esc(p.join(':').trim()||f)}</td></tr>`}).join('')}</table></div>`;$('#whyImportant').textContent=x.whyImportant||'This update is relevant to current affairs and general knowledge.';$('#articleSources').innerHTML=(x.sources||[]).map(s=>`<a class="source-item" target="_blank" rel="noopener" href="${esc(s.url)}">▣ ${esc(s.name||s.url)}</a>`).join('')||'<small>No source links available.</small>';renderQuiz(x)}
function renderQuiz(x){const key=(x.keyFacts||[])[0]||x.category;$('#articleQuiz').innerHTML=`<p style="font-size:11px"><b>Which topic is this article mainly related to?</b></p>${[x.category,'World','Sports','History'].slice(0,4).map((o,i)=>`<button class="quiz-option" data-q="${i}"><span class="quiz-radio"></span><span>${esc(o)}</span></button>`).join('')}<button class="primary-btn wide" id="showAnswer">Show Answer</button><div class="quiz-result" id="articleQuizResult"></div>`;document.querySelectorAll('[data-q]').forEach(b=>b.onclick=()=>document.querySelectorAll('[data-q]').forEach(z=>z.classList.toggle('selected',z===b)));$('#showAnswer').onclick=()=>$('#articleQuizResult').textContent='Answer: '+x.category}
load();
